# 连续与可导的关系

#数学二 #高等数学 #第二章 #知识点/连续性 #知识点/可导性 #知识点/可微性

## 🏷️ 标签体系
- **L1知识点**：`连续与可导的关系` `可导与可微的关系` `连续不可导`
- **L2方法**：`关系判断法` `反例构造法`
- **L3章节**：`一元函数微分学`
- **难度**：`hard`

## 📖 连续、可导、可微之间的关系（原书第67页）

### 🔑 基本关系定理

对于一元函数 $y = f(x)$：

1. **可导必连续**：若 $f(x)$ 在点 $x_0$ 处可导，则 $f(x)$ 在点 $x_0$ 处必连续
2. **连续不一定可导**：$f(x)$ 在点 $x_0$ 处连续，但在点 $x_0$ 处不一定可导
3. **可导与可微等价**：$f(x)$ 在点 $x_0$ 处可导 $\Leftrightarrow$ $f(x)$ 在点 $x_0$ 处可微

### 💡 关系图示
```
可微 ⟺ 可导 ⟹ 连续
     ⇍ (反之不成立)
```

## 一、可导必连续的证明

设 $f(x)$ 在点 $x_0$ 处可导，即 $\lim_{\Delta x \to 0} \frac{f(x_0 + \Delta x) - f(x_0)}{\Delta x} = f'(x_0)$ 存在。

则：
$$\begin{aligned}
\lim_{\Delta x \to 0} [f(x_0 + \Delta x) - f(x_0)] &= \lim_{\Delta x \to 0} \frac{f(x_0 + \Delta x) - f(x_0)}{\Delta x} \cdot \Delta x\\
&= f'(x_0) \cdot 0 = 0
\end{aligned}$$

所以 $\lim_{\Delta x \to 0} f(x_0 + \Delta x) = f(x_0)$，即 $f(x)$ 在点 $x_0$ 处连续。

## 二、连续不一定可导的例子

### 📐 经典反例
$f(x) = |x|$ 在 $x = 0$ 处连续但不可导。

**证明**：
- 连续性：$\lim_{x \to 0} |x| = 0 = |0|$，所以在 $x = 0$ 处连续
- 可导性：
  - $f'_-(0) = \lim_{x \to 0^-} \frac{|x| - |0|}{x - 0} = \lim_{x \to 0^-} \frac{-x}{x} = -1$
  - $f'_+(0) = \lim_{x \to 0^+} \frac{|x| - |0|}{x - 0} = \lim_{x \to 0^+} \frac{x}{x} = 1$
  
  因为 $f'_-(0) \neq f'_+(0)$，所以 $f(x) = |x|$ 在 $x = 0$ 处不可导。

### 📐 其他常见例子

1. **尖点型**：$f(x) = \sqrt[3]{x^2}$ 在 $x = 0$ 处连续但不可导（导数为无穷大）

2. **振荡型**：$f(x) = \begin{cases}
x\sin\frac{1}{x}, & x \neq 0\\
0, & x = 0
\end{cases}$ 在 $x = 0$ 处连续但不可导

## 三、可导与可微的等价性

### 充分性
若 $f(x)$ 在点 $x_0$ 处可导，设 $f'(x_0) = A$，则：
$$\lim_{\Delta x \to 0} \frac{f(x_0 + \Delta x) - f(x_0) - A\Delta x}{\Delta x} = \lim_{\Delta x \to 0} \left[\frac{f(x_0 + \Delta x) - f(x_0)}{\Delta x} - A\right] = f'(x_0) - A = 0$$

所以 $f(x_0 + \Delta x) - f(x_0) - A\Delta x = o(\Delta x)$，即 $\Delta y = A\Delta x + o(\Delta x)$

因此 $f(x)$ 在点 $x_0$ 处可微。

### 必要性
若 $f(x)$ 在点 $x_0$ 处可微，则 $\Delta y = A\Delta x + o(\Delta x)$，于是：
$$\lim_{\Delta x \to 0} \frac{\Delta y}{\Delta x} = \lim_{\Delta x \to 0} \frac{A\Delta x + o(\Delta x)}{\Delta x} = A$$

所以 $f'(x_0) = A$ 存在，即 $f(x)$ 在点 $x_0$ 处可导。

## 📐 综合例题

### 例1：分段函数的连续性与可导性

**例**：设 $f(x) = \begin{cases}
x^2, & x \leq 1\\
ax + b, & x > 1
\end{cases}$，问 $a$、$b$ 取何值时，$f(x)$ 在 $x = 1$ 处：
(1) 连续？
(2) 可导？

**解**：
(1) 连续性要求：$\lim_{x \to 1^-} f(x) = \lim_{x \to 1^+} f(x) = f(1)$

$$\lim_{x \to 1^-} f(x) = \lim_{x \to 1^-} x^2 = 1$$
$$\lim_{x \to 1^+} f(x) = \lim_{x \to 1^+} (ax + b) = a + b$$
$$f(1) = 1$$

所以连续要求：$a + b = 1$

(2) 可导性要求：$f'_-(1) = f'_+(1)$

$$f'_-(1) = \lim_{x \to 1^-} \frac{x^2 - 1}{x - 1} = \lim_{x \to 1^-} (x + 1) = 2$$
$$f'_+(1) = \lim_{x \to 1^+} \frac{ax + b - 1}{x - 1} = \lim_{x \to 1^+} \frac{a(x - 1) + (a + b - 1)}{x - 1} = a$$

所以可导要求：$a = 2$，结合连续条件得 $b = -1$

### 例2：带振荡项的函数（原书例36）

设函数 $f(x) = \begin{cases}
\sin 2x + x^2\cos\frac{1}{x}, & x \neq 0\\
0, & x = 0
\end{cases}$，判断 $f(x)$ 在 $x = 0$ 处的连续性与可导性。

**解**：
由导数的定义：
$$f'(0) = \lim_{x \to 0} \frac{f(x) - f(0)}{x - 0} = \lim_{x \to 0} \frac{\sin 2x + x^2\cos\frac{1}{x}}{x}$$

$$= \lim_{x \to 0} \frac{\sin 2x}{x} + \lim_{x \to 0} x\cos\frac{1}{x} = 2 + 0 = 2$$

因此 $f(x)$ 在 $x = 0$ 处可导，从而也连续、可微。

### 例3：分段函数的综合判断（原书例53）

设函数 $f(x) = \begin{cases}
x^2, & x < 1\\
ax + b, & x \geq 1
\end{cases}$，求 $a, b$ 使得 $f(x)$ 在 $x = 1$ 处可导。

**解**：
因可导必连续，由 $f(x)$ 在 $x = 1$ 处可导，则 $f(x)$ 在 $x = 1$ 处必连续。

连续性条件：
$$\lim_{x \to 1^-} f(x) = \lim_{x \to 1^-} x^2 = 1$$
$$\lim_{x \to 1^+} f(x) = \lim_{x \to 1^+} (ax + b) = a + b$$
$$f(1) = a + b$$

连续要求：$a + b = 1$

可导性条件：
$$f'_-(1) = \lim_{x \to 1^-} \frac{x^2 - 1}{x - 1} = \lim_{x \to 1^-} (x + 1) = 2$$
$$f'_+(1) = \lim_{x \to 1^+} \frac{ax + b - (a + b)}{x - 1} = a$$

可导要求：$f'_-(1) = f'_+(1)$，即 $a = 2$

因此：$a = 2, b = -1$

### 例4：含绝对值的复合函数

设 $f(x) = |x - 1|(x^2 - 1)$，讨论 $f(x)$ 在 $x = 1$ 处的可导性。

**解**：
首先改写函数：
$$f(x) = |x - 1|(x^2 - 1) = |x - 1|(x - 1)(x + 1)$$

当 $x < 1$ 时：$f(x) = -(x - 1)^2(x + 1)$
当 $x > 1$ 时：$f(x) = (x - 1)^2(x + 1)$
当 $x = 1$ 时：$f(1) = 0$

计算单侧导数：
$$f'_-(1) = \lim_{x \to 1^-} \frac{-(x - 1)^2(x + 1) - 0}{x - 1} = \lim_{x \to 1^-} -(x - 1)(x + 1) = 0$$
$$f'_+(1) = \lim_{x \to 1^+} \frac{(x - 1)^2(x + 1) - 0}{x - 1} = \lim_{x \to 1^+} (x - 1)(x + 1) = 0$$

因为 $f'_-(1) = f'_+(1) = 0$，所以 $f(x)$ 在 $x = 1$ 处可导，且 $f'(1) = 0$。

## 🎯 易错提醒

### 核心关系记忆
1. **可导一定连续，但连续不一定可导**
2. **不连续一定不可导**（逆否命题）
3. **对于一元函数，可导与可微是等价的**
4. **左右导数都存在且相等 ⇔ 可导**

### 常见误区
1. **误区**：连续就可导
   - **反例**：$f(x) = |x|$ 在 $x = 0$ 处连续但不可导

2. **误区**：左右导数都存在就可导
   - **正确**：左右导数都存在且相等才可导

3. **误区**：可导点的导数值一定有限
   - **注意**：导数存在意味着是有限值，无穷大表示不可导

### 判断步骤规范
对于分段函数在分段点 $x_0$ 的可导性判断：
1. **第一步**：检查连续性（可导的必要条件）
2. **第二步**：若连续，分别计算左右导数
3. **第三步**：比较左右导数是否相等

## 🔗 相关链接
- [[连续性的概念]]
- [[导数的定义]]
- [[微分的定义]]

## 🔗 相关题目练习
根据标签可在题库中搜索：
- 知识点标签：`连续性` `可导性` `可微性`
- 章节标签：`一元函数微分学`
- 难度：`hard`